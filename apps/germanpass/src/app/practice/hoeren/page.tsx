import { PracticeRunner } from "@/components/practice-runner";

export const metadata = { title: "Entraînement Hören" };

export default function HoerenPage() {
  return <PracticeRunner section="HOEREN" />;
}
