import { PracticeRunner } from "@/components/practice-runner";

export const metadata = { title: "Entraînement Lesen" };

export default function LesenPage() {
  return <PracticeRunner section="LESEN" />;
}
